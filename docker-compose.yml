version: "3.8"
services:
  database:
    image: "postgres:12-alpine"
    ports:
      - 5432:5432
    # volumes:
    #   - db-data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: tom-db
      POSTGRES_USER: dev_user
      POSTGRES_PASSWORD: dev_password
# api:
#   build: ./api
#   depends_on:
#     - database
#   environment:
#     PORT: 3000
#     POSTGRES_URL: mongodb://db_mongo/hypertube