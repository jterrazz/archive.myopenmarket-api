import Router from 'koa-router';

const orderRouter = new Router();

// orderRouter
// .post('/', ); // which contract
// .get('/', );
// .get('/:orderId', );
// .patch('/:orderId', );
// .delete('/:orderId', );

export default orderRouter;
