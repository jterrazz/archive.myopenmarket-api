import Router from 'koa-router';
// import { isAuthenticated } from '@middlewares/authentication';

const contractsRouter = new Router();

// contractsRouter
// .get('/:contractId', );
// isAuthenticated.post('/:contractId/register', );

export default contractsRouter;
