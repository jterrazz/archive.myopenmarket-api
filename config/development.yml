api:
  http:
    port: 3000
  auth:
    jwt-signature: 'dev_jwt'
database:
  postgres:
    host: '127.0.0.1'
    port: 5432
    username: 'dev_user'
    password: 'dev_password'
    database: 'tom-db'
services:
  elastic:
  mixpanel:
